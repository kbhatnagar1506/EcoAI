# Runtime dependencies. Pinned for reproducible deploys.
# Development and test tooling lives in requirements-dev.txt.

Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
Flask-Login==0.6.3
Flask-WTF==1.2.1
Flask-Limiter==3.8.0
SQLAlchemy==2.0.35
alembic==1.13.3
WTForms==3.1.2
email-validator==2.2.0

# OAuth client (Google sign-in). Inert unless GOOGLE_CLIENT_ID is configured.
Authlib==1.3.2

# Loads .env in development. In production the platform supplies real env vars.
python-dotenv==1.0.1

# PostgreSQL driver. psycopg3 has wheels for Python 3.11 through 3.13;
# psycopg2-binary does not build on 3.13.
psycopg[binary]==3.2.3

gunicorn==23.0.0
requests==2.33.0
